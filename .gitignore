.DS_Store
*.swp
*~
node_modules/

# Parliament Thesaurus crawler cache (per-URL Turtle files; transient).
cache-parliament-lda-terms/

# Temporary while initial thesaurus crawl is in progress (will be
# removed in the same commit that adds parliament-lda-terms.nq.gz).
third_party/data/parliament-lda-terms/
