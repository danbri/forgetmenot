# Per-site crawl output is generated by `parl members crawl-sites`.
# This .gitignore is in place ONLY while the initial crawl is in
# flight, to avoid committing a half-written snapshot. Once the
# full run completes, this file is removed in the same commit
# that adds the data.
*/
